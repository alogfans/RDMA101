# RDMA101 写作准则

本文档记录 RDMA101 后续写作、改写和制图时需要遵守的准则。它面向参与本项目写作的人，也面向协助维护文档的 AI 编程助手。

## 1. 写作对象

RDMA101 的正文应面向正在学习、部署、调试或研究 RDMA 系统的读者，而不是面向提问者作即时回答。写作时应避免使用“我来说明”“下面我们看”“你需要知道”这类对话式表达。

正文可以使用“本篇”“本节”“后续章节”等出版物表达，但应避免把段落写成任务说明或回答提纲。每一节都应服务于读者理解问题本身，而不是展示写作者的解释过程。

## 2. 章节职责

每一篇文章应先确定本篇解决什么问题，再确定各节之间的论证关系。章节不能只是概念罗列。

第一篇已经形成的结构可作为后续写作参照：

- 先建立共同背景，例如网络编程的一般模式；
- 再解释已有机制，例如 TCP socket 如何实现这一模式；
- 再指出边界和动机，例如 TCP 数据路径在高性能数据面中的开销；
- 然后引入 RDMA 的关键特点；
- 最后用工具、实验和代码样例把概念落到可运行对象上。

后续篇章也应保持类似层次：先说明问题，再引入对象，再解释机制，最后回到工程现象、实验或系统案例。

## 3. 术语引入

术语必须按读者需要逐步引入。第一章只解释运行和理解 Hello World 必需的概念，不应提前展开第二章才需要完整讨论的对象模型。

写 RDMA 概念时，应先给出可理解的直观含义，再给出必要的正式名称。例如先解释“提交请求”“完成记录”，再引入 WR、CQ、WC 等缩写。缩写第一次出现时应写出英文全称或中文解释。

避免一次性堆叠 MR、QP、CQ、PD、WR、SGE、lkey、rkey、GID、PSN 等术语。若某个术语只是为了阅读命令输出或代码样例而出现，应限定解释范围，并说明完整关系将在后续章节展开。

## 4. 技术表述

技术判断应准确、有边界。不要把 RDMA 写成 TCP 的替代品，也不要把 kernel bypass 写成完全不经过内核，把 zero copy 写成系统中没有数据移动，把 one-sided 写成完全不需要远端协作。

表达性能动机时，应使用具体目标和场景，例如 GB/s 级带宽、微秒级延迟、大块数据搬运、高频小消息、CPU 开销、NUMA 和 RoCE 环境等。避免空泛说法，例如“极致性能”“大幅提升”“非常快”。

涉及真实环境时，应区分：

- InfiniBand 与 RoCE 的网络差异；
- 真实 RDMA 网卡与 Soft-RoCE/RXE 的能力边界；
- 控制面与数据面；
- 程序提交成功、网卡完成、远端内存可见、远端应用已处理之间的语义差异；
- 工具测试结果与严肃基准测试之间的差异。

## 5. 文字风格

正文应采用清楚、平实、可出版的技术写作风格。句子宜短，段落宜有明确中心。少用抽象总结，多用具体场景、命令、代码和输出说明问题。

避免以下写法：

- “本篇的目标是……”式的课程宣传腔；
- “不仅仅是……而是……”的连续排比；
- “一套完整体系”“深度理解”“打通认知”等空泛表达；
- 为了显得完整而提前加入过多概念；
- 对读者发号施令式的口吻。

可以使用以下写法：

- “TCP socket 用 IP 地址和端口描述 endpoint。”
- “`ibv_post_send` 返回成功，只说明请求已经被接受投递。”
- “RXE 适合学习 API 和通信流程，但不能代表真实网卡性能。”

## 6. 示例与输出

教程中的命令应尽量给出可直接运行的形式。占位符只有在不可避免时使用；能用 `eth0`、`mlx5_0`、`172.31.32.3` 这类示例值说明时，不使用抽象的 `<netdev>`、`<device>`、`<server-ip>`。

工具输出应来自真实或接近真实的环境，并可适当脱敏。输出后要说明读者应关注哪些字段，避免只贴日志不解释。

第一篇中形成的工具顺序应继续沿用：

1. `ibv_devices` 查看 RDMA 设备列表；
2. `ibv_devinfo` 查看设备和端口参数；
3. `perftest` 完成第一次传输测试。

第一次 perftest 不强行指定 GID index。优先相信工具默认选择；只有默认选择失败或需要严格复现实验条件时，再显式指定参数。

## 7. 代码样例

代码样例应服务于概念，不追求工程框架完整。第一篇的样例应保持最小：TCP 控制通道交换连接信息，RDMA 数据面完成一次 one-sided WRITE。

示例代码应能独立编译运行，放在 `examples/` 目录下，并提供 Makefile。源码文件头部应写明代码许可，当前代码使用 Apache License 2.0：

```text
Copyright 2026 Feng Ren
Licensed under the Apache License, Version 2.0.
SPDX-License-Identifier: Apache-2.0
```

文档正文可以解释样例删去了哪些工程复杂度，但不应把线程池、多网卡调度、metadata 管理、错误恢复等内容提前展开到第一篇。

## 8. 图示

图示应解释结构或流程，不做装饰。优先使用 Mermaid 写在 Markdown 中，由 MkDocs 构建流程自动渲染。

适合放图的位置包括：

- 生命周期：例如 TCP socket 的 `socket`、`bind`、`listen`、`accept`、`connect`、`send`、`recv`；
- 数据路径：例如 TCP 数据路径与 RDMA 数据路径对比；
- 队列模型：例如 QP、send queue、receive queue、CQ 与 completion；
- 样例流程：例如 TCP 控制面交换信息，RDMA 数据面写入远端 buffer。

图应靠近第一次需要它的正文。图中术语应与正文一致，不引入正文尚未解释的新对象。

Mermaid 图不宜过大。站点通过 `docs/stylesheets/extra.css` 统一限制图的最大宽度；新增图时应优先控制节点数量和文字长度，避免用一张图承载过多信息。复杂流程应拆成多张小图。

## 9. 文档与代码许可

README 和首页应统一说明项目许可边界。文档材料遵循项目文档许可；示例代码或软件组件如果有单独文件头声明，则以文件头声明为准。

涉及社区贡献时，应保留作者基于全部内容整理、改写并独著出版的权利说明，同时明确公开文档版本会持续保留。

## 10. 验证要求

修改文档后至少运行：

```bash
mkdocs build --strict
git diff --check
```

修改示例代码后还应运行对应目录的编译命令，例如：

```bash
make -C examples/one_sided_write
make -C examples/one_sided_write clean
```

构建产生的 `site/` 和示例二进制不应作为正文修改的一部分提交，除非发布流程明确要求。
